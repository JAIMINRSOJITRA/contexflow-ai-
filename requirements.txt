# Core API
fastapi==0.141.1
uvicorn==0.52.0
python-multipart==0.0.32
pydantic==2.13.4

# Database
SQLAlchemy==2.0.51

# AI providers — direct SDKs, no LangChain in the main pipeline
google-genai==2.16.0
groq==0.37.1

# Document processing
pymupdf==1.28.0
python-docx==1.2.0

# Vector store
faiss-cpu==1.14.3
numpy==2.5.1

# Local embedding fallback (no API key required)
sentence-transformers>=3.0.0

# Utilities
python-dotenv==1.2.2

# Ragas evaluation — langchain-google-genai is kept here ONLY because
# Ragas requires LangChain-compatible objects as its internal judge models.
# It is not used anywhere else in the codebase.
langchain-google-genai==4.3.2
ragas>=0.3.9,<0.4

# Test runner
pytest==9.1.1
